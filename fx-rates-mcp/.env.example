# Базовый URL FX API (по умолчанию используем exchangerate.host)
FX_API_BASE=https://api.exchangerate.host/latest

# Таймаут HTTP-запросов к FX API (секунды)
FX_HTTP_TIMEOUT=10.0

# Базовая валюта по умолчанию (если понадобится)
FX_DEFAULT_BASE_CURRENCY=RUB

# Настройки MCP сервера
HOST=0.0.0.0
PORT=8001

# Логирование / трейсинг (при необходимости)
LOG_LEVEL=INFO
